# Comma-separated API keys (required for MCP tool calls)
API_KEYS=dev-key-1,dev-key-2

# HTTP listen port (default 3000)
PORT=3000

# Bind host (0.0.0.0 for Docker; 127.0.0.1 for local-only)
HOST=0.0.0.0

# Optional: enable live web fetch for research_brief depth=quick
LIVE_RESEARCH=0
